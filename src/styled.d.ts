import "styled-components";
declare module "styled-components" {
  export interface DefaultTheme {
    containorColor: string;
    textColor: string;
    bgColor: string;
    accentColor: string;
    borderColor: string;
  }
}
